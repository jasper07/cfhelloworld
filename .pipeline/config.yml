---
general:
  buildTool: "mta"

# Steps configuration
steps:
  mtaBuild:
    buildTarget: "CF"
  cloudFoundryDeploy:
    deployTool: "mtaDeployPlugin"
    apiEndpoint: "https://api.cf.ap10.hana.ondemand.com"
    org: "secondphase"
    space: "dev"

  # tmsUpload:
  #   nodeName: "QA_MTA"
  #   credentialsId: "tms-sk"
